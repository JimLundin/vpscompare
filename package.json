{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "vitest --config vitest.config.e2e.ts",
    "test:e2e:run": "vitest run --config vitest.config.e2e.ts",
    "test:e2e:coverage": "vitest run --coverage --config vitest.config.e2e.ts"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.12.8",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.7",
    "happy-dom": "^20.0.10",
    "vitest": "^4.0.7"
  }
}
